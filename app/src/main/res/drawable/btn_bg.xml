<!-- res/drawable/custom_button.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- State when button is pressed -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#1B4242" /> <!-- Change to the desired pressed color -->
            <corners android:radius="12dp" /> <!-- Adjust the corner radius as needed -->
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape>
            <solid android:color="#78A083" /> <!-- Change to the desired default color -->
            <corners android:radius="12dp" /> <!-- Adjust the corner radius as needed -->
        </shape>
    </item>

</selector>
